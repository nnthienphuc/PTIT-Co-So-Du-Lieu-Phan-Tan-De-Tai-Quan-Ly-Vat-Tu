CREATE PROC sp_TraCuu_KiemTraMaVatTu
@MAVT nchar(4)
AS
BEGIN
	IF EXISTS(SELECT 1 
			  FROM Vattu VT  
			  WHERE VT.MAVT = @MAVT)
			RETURN 1; 
	ELSE IF EXISTS(SELECT 1
				   FROM LINK1.QLVT_DATHANG.DBO.Vattu VT  
				   WHERE VT.MAVT = @MAVT)
			RETURN 1; 
	RETURN 0;
END